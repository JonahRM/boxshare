<h1>Listing specifications</h1>

<table>
  <thead>
    <tr>
      <th>Comment</th>
      <th>Will use</th>
      <th>User</th>
      <th>Game</th>
      <th>Group</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @specifications.each do |specification| %>
      <tr>
        <td><%= specification.comment %></td>
        <td><%= specification.will_use %></td>
        <td><%= specification.user_id %></td>
        <td><%= specification.game_id %></td>
        <td><%= specification.group_id %></td>
        <td><%= link_to 'Show', specification %></td>
        <td><%= link_to 'Edit', edit_specification_path(specification) %></td>
        <td><%= link_to 'Destroy', specification, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Specification', new_specification_path %>
